document.addEventListener('DOMContentLoaded', function() {
  if (document.querySelector('body').classList.contains('index-body')) {
    // Code
  } else if (!document.querySelector('body').classList.contains('about-body')) {
    // Code
  } else {
    // Code
  }
});
